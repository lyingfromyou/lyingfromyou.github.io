# 性能优化

> 性能优化就是让机器发挥其本身的性能



1. 到底是不是问题
2. 是问题， 怎么证明是问题
3. 梳理思路



#### Java程序导致cpu飙升，定位问题

1. `top`命令，如果是java占用cpu，找到对应占用cpu的java进程的pid
2.  `jstack {pid} > a.txt` 将进程的堆栈信息输出到文件，`jstack` 是JDK自带的堆栈分析工具
3. 执行  `top -p {pid} -H`  找到进程内占用cup高的线程
4. `printf "%x \n" {pid}` 将最耗cpu的线程id转换为16进制输出
5. 用输出的16进制的信息到 a.txt 文件中找到对应信息，就可以看到线程的状态等相关信息



查看内存

`free -m` 

`free -g` 



查看IO 

`iostat -dx 1`

`du -f` 查看文件夹/文件 大小



压测工具 `Jmeter`







- 吞吐量
- 平均响应时间
- tps
  - Queries Per Second意思是“每秒查询率”，是一台服务器每秒能够相应的查询次数，是对一个特定的查询服务器在规定时间内所处理流量多少的衡量标准。
- qps
  - TransactionsPerSecond的缩写，也就是事务数/秒。它是软件测试结果的测量单位。一个事务是指一个客户机向服务器发送请求然后服务器做出反应的过程。客户机在发送请求时开始计时，收到服务器响应后结束计时，以此来计算使用的时间和完成的事务个数，





